namespace Game;
public class LevelCompletionData
{
    //right now this is just getting a bool if the level was completed
    // but more data could be added like amount of time
    // or the amount of resource left
    public bool IsCompleted { get; set; }
}